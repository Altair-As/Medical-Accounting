@page
@model WebApplicationAuth.Pages.Administration.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Administration/Shared/_Layout.cshtml";
}

<div class="container">


    <div class="row text-center">

        <h1 class="display-4">Illneses list</h1>

    </div>

    <div class="row justify-content-center mb-4 mt-2">

        <div class="col-auto align-self-center">

            <a asp-page="Create" class="btn btn-primary pt-2 border border-1 border-dark addbtn">Add new illnes</a>

        </div>

    </div>

</div>


<table class="table table-striped table-bordered text-center" style="vertical-align:middle">
    <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChronicIllness[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChronicIllness[0].Code)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ChronicIllness) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
